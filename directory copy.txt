Hogyan lehet másolni a fájlokat a források egy másik könyvtárba...



Hide   Copy Code
System.Drawing.Icon ikon = Properties.Resources.icons;
              Patak IconStream = System.IO.File.OpenWrite (path + "\\ \\ ikonok test.ico");
              icon.Save (IconStream);



Ez jól mûködik egy ikon, de az alábbi kód nem mûködik az egyik segítségével fájlokat; teremt, de amikor ránézek a fájlt úgy tûnik, hogy üres, és szokás nyitott
Hide   Copy Code
  író = új StreamWriter (path + "\\ Segíts \\ \\ en test.chm");
writer.Write (Properties.Resources.test);



   	
és amely mûködnie kell.

Hide   Copy Code
karakterlánc [] fájlneveket = Assembly.GetExecutingAssembly (). GetManifestResourceNames ();
 foreach (string fileName a fájlneveket)
{
    felhasználásával (FileStream FILESTREAM = File.Create (@ "c: \ temp \" + fileName))
    {
Assembly.GetExecutingAssembly (). GetManifestResourceStream (fileName) .CopyTo (FileStream);
    }
}

